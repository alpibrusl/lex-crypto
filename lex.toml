[package]
name        = "lex-crypto"
version     = "0.1.0"
description = "Higher-level cryptographic library for Lex: JWT, OAuth2/PKCE, Argon2id, signed/sealed cookies, webhook verification, TOTP"
lex         = "0.9.2"

[dependencies]
lex-schema = { git = "https://github.com/alpibrusl/lex-schema" }
